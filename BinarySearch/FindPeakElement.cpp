#include<bits/stdc++.h>
using namespace std;

int peakElement(vector<int>&arr){
    int n = arr.size();
    int low = 0, mid, high = n-1;
    while(low <= high){
        mid = (high + low)/2;
        if(arr[mid] ){
            
        }
    }
}


int main(){
    vector<int> arr = {1, 1, 2, 2, 3, 3, 4, 4, 5};
    int n = arr.size(), x = 3;

}