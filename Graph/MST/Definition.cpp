/*
    It should be undirected.
    Minimim spanning tree means the tree is having N nodes and N - 1 edges.
    And all nodes are reachable from eachother.
    The tree which is having minimum sum is called MST.
*/